# ============================================================================
# PAW Development Environment Configuration Template
# ============================================================================
# SECURITY WARNING: This is a template file. Copy to .env and set secure values.
# DO NOT commit .env files with real credentials to version control.
#
# Usage:
#   1. Copy this file: cp .env.example .env
#   2. Update values with secure passwords
#   3. Ensure .env is in .gitignore (it already is)
#
# ============================================================================

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================
# Database password for the 'paw' user
# IMPORTANT: Use a strong, unique password in production or exposed environments
POSTGRES_PASSWORD=changeme

# ============================================================================
# Grafana Configuration
# ============================================================================
# Grafana admin user credentials
# Default username (can be customized)
GRAFANA_ADMIN_USER=admin

# Grafana admin password
# IMPORTANT: Change this to a strong, unique password
GRAFANA_ADMIN_PASSWORD=changeme

# ============================================================================
# Additional Security Notes
# ============================================================================
# 1. Generate strong passwords using: openssl rand -base64 32
# 2. Never commit the .env file to version control
# 3. Rotate passwords regularly in production environments
# 4. Use secrets management systems (Vault, AWS Secrets Manager) for production
# 5. Restrict network access to these services using firewalls/security groups
# ============================================================================
