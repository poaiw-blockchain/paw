# Testnet Stats Dashboard

Static dashboard that surfaces live RPC-derived stats:

- Latest height
- Average block time (last 50 blocks)
- TPS estimate
- Validator count

Deployed at `https://testnet-explorer.poaiw.org/stats/` and linked from the Explorer landing page.
